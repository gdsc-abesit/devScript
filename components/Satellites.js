import styles from './Satellites.module.scss'

export default function Satellites() {
  return (
    <div className={styles.wrapper}>
      Can't make it to the venue? <a href="">Join us online!</a>
    </div>
  )
}
